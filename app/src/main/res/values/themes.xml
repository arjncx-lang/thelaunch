<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Transparent system bars (same look as most modern apps):
      - Do NOT use windowIsTranslucent — OEMs (e.g. Transsion Taskbar) often force a
        solid/scrim nav bar for translucent HOME windows, so buttons look "blocked".
      - Draw app content edge-to-edge under the 3-button bar; disable contrast scrim.
    -->
    <style name="Theme.Thelaunch" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">#FF000000</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <style name="Theme.Thelaunch.Settings" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">#FF000000</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
